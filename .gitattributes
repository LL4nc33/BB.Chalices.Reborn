# Normalise line endings; let git decide text vs binary by default
* text=auto

# Shell scripts must stay LF (also when checked out on Windows/WSL)
*.sh text eol=lf

# Treat these as binary (no eol munging / diff noise)
*.ttf binary
*.otf binary
*.ico binary
*.png binary
*.jpg binary
*.db binary
